# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node / build
node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri / Rust build artifacts (large, regenerable)
src-tauri/target/
src-tauri/gen/

# OS / IDE caches
Thumbs.db
desktop.ini

# Secrets (never commit API keys)
.env
.env.local
.env.*.local
*.secret
*.key

# Local Tauri dev signing keys (if generated)
src-tauri/.tauri/

# Live2D character sources we license but don't redistribute publicly
# (uncomment when we commission commercial models)
# src-tauri/resources/characters/commercial/
